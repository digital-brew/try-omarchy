# Chromium composites through VirGL, which cannot give it the GPU path it
# expects, so it renders in software on the VirGL guest. Deleting this file
# re-enables the GPU path.
--ozone-platform=wayland
--disable-gpu
